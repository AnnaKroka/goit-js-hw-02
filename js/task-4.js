function getShippingCost(country) {
    let price = 2;
    let message = '';
    switch (price) {
        case 1: {
            message = `Shipping to ${country} will cost ${price} credits`;
            break;
        }
    }
}